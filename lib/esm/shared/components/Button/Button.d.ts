import { ReactNode } from "react";
interface Props {
    className?: string;
    children: ReactNode | ReactNode[];
}
export declare const Button: ({ className, children }: Props) => JSX.Element;
export {};
